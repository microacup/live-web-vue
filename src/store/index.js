import Vue from 'vue';
import Vuex from 'vuex';
import api from './api';

Vue.use(Vuex);


const store = new Vuex.Store({
  state: {
  },

  actions: {
  },

  mutations: {
  },

  getters: {
  }
});

export default store;